/// On-device TFLite MLP classifier over landmark features.
/// Load [.tflite] asset and run inference in a later step.
abstract final class TfliteClassifier {
  static Future<void> dispose() async {}
}
