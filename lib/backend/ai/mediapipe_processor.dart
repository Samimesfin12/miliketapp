/// Hand landmarks from camera frames (MediaPipe-style pipeline).
/// Run heavy work in an [Isolate]; implement in a later step.
abstract final class MediaPipeProcessor {
  static Future<void> dispose() async {}
}
