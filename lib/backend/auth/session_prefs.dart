/// Keys for on-device session (UUID token + user id in SharedPreferences).
abstract final class SessionPrefs {
  static const String tokenKey = 'session_token';
  static const String userIdKey = 'session_user_id';
}
